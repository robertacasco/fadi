---
export const prerender = false;

const slug = Astro.params.slug;
const destination = slug
  ? `https://admin.annuncifunebri.it/famiglia/${encodeURIComponent(slug)}`
  : '/area-famiglia/';

return Astro.redirect(destination, 302);
---
